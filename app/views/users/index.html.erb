<style>
table
{
  border-collapse:collapse
}
table, td, th
{
border:1px solid black;
}
</style>

<table style="border:groove 3px black; border-collapse:collapse;">
  <tr>
    <th style ="padding:15px">user_email</th>
    <th style = "padding:6px">User_id</th>
    <th style = "padding:6px" colspan="6">Actions</th>
  </tr>
 
  <% @user.each do |user| %>
    <tr>
      <td style ="color:SaddleBrown;padding:5px"><%= user.email %></td>
      <td style ="color:MidnightBlue;padding:10px"><%= user.id %></td>
      <td style="padding:5px"><%= link_to 'Show', user_path(user) %></td>
      <td style="padding:5px"><%= link_to 'Edit', edit_user_path(user) %></td>
      <td style="padding:5px"><%= button_to 'delete', user_path(user), method: :delete, :confirm => :"Are you sure" %> </td>
    </tr>
  <% end %>
</table>
<p>
<%= link_to 'Create New Article', new_article_path %></td> 
</p>  

<%= current_user.id %> 